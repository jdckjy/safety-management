# 제주 지역별 인구동향 분석 시스템 (Jeju Pop-Radar) 기능 구현 분석

---

## 0. 기존 시스템 아키텍처 기반의 KOSIS 연동 심층 분석 (추가)

이전 섹션이 일반적인 제품 요구사항(PRD) 관점이었다면, 이 섹션은 현재 구현된 **클라이언트 중심의 정적 데이터 애플리케이션**에서 **외부 API 연동 기반의 동적 데이터 애플리케이션**으로 확장하기 위한 구체적인 기술 과제와 실행 계획을 다룹니다.

### 가. 현행 시스템 아키텍처 분석

1.  **프레임워크 및 언어**: `React`와 `TypeScript`를 기반으로 하는 SPA(Single Page Application)입니다.
2.  **데이터 처리 방식**: `src/data/*.json`, `src/data/*.ts` 와 같이 프로젝트 내에 **정적(Static) 데이터**를 포함시키고, 이를 직접 `import` 하여 사용하는 구조입니다. 현재 외부 API를 호출하여 동적으로 데이터를 가져오는 기능은 전무합니다.
3.  **UI 구성**: `src/components/ui`에 정의된 자체 UI 컴포넌트(예: `Tabs`, `Table`)를 활용하고 있습니다. 통계 정보 시각화를 위한 차트, 지도 등의 라이브러리는 아직 도입되지 않았습니다.
4.  **라우팅**: `react-router-dom`과 같은 표준 라이브러리 대신, `MainLayout.tsx`에서 상위 상태(`navigationState`)에 따라 조건부로 컴포넌트를 렌더링하는 **내부 탐색 로직**을 사용하고 있습니다.
5.  **백엔드**: 별도의 백엔드 서버 없이, `vite`를 통해 빌드된 정적 파일(HTML, CSS, JS)을 서빙하는 순수 **프론트엔드 애플리케이션**입니다.

### 나. 신규 기능 구현을 위한 핵심 기술 과제 (Challenges)

현행 아키텍처에서 KOSIS API 연동 기능을 구현하기 위해서는 다음과 같은 기술적 과제를 반드시 해결해야 합니다.

1.  **[과제 1: CORS 정책 및 백엔드 부재]**
    *   **문제**: 브라우저(클라이언트)에서 KOSIS와 같은 외부 API를 직접 호출하면, 보안 정책인 **CORS(Cross-Origin Resource Sharing)** 에 의해 거의 항상 차단됩니다.
    *   **해결책 (필수)**: 이 문제를 우회하기 위해 **백엔드 프록시(Proxy) 서버** 도입이 시급합니다. 프론트엔드는 우리 소유의 백엔드를 호출하고, 그 백엔드가 KOSIS API와 통신하여 결과를 프론트엔드에 전달하는 중계 역할을 해야 합니다.
    *   **실행 방안**: 경량 `Node.js/Express` 서버를 구축하거나, 배포 환경(예: Vercel, Netlify)에서 제공하는 **서버리스 함수(Serverless Function)**를 활용하여 프록시를 구현합니다.

2.  **[과제 2: 동적 데이터 패칭 로직 부재]**
    *   **문제**: 현재는 정적 데이터를 사용하므로 비동기 데이터 로딩, 로딩 상태, 에러 상태 등을 전혀 고려하고 있지 않습니다.
    *   **해결책 (필수)**: `PopulationInfoTab.tsx`와 같은 컴포넌트 내에서 백엔드 프록시 API를 호출하는 **비동기 데이터 패칭(Fetching) 로직**이 필요합니다.
    *   **실행 방안**: `useEffect` 훅 내에서 `fetch` 또는 `axios` 라이브러리를 사용하여 API를 호출합니다. 데이터가 로딩 중일 때는 스피너나 스켈레톤 UI를, API 호출 실패 시에는 에러 메시지를 사용자에게 보여주는 상태 처리가 반드시 병행되어야 합니다.

3.  **[과제 3: 데이터 시각화 기능 부재]**
    *   **문제**: 현재 시스템에는 수치 데이터를 직관적인 그래프나 지도로 변환하는 시각화 라이브러리가 없습니다.
    *   **해결책 (필수)**: KOSIS에서 받은 인구 통계 데이터를 시계열 차트나 지도 형태로 보여주기 위한 **시각화 라이브러리 도입**이 필요합니다.
    *   **실행 방안**:
        *   **차트**: `Recharts`, `Nivo`, `Chart.js` 등 React와 호환성이 좋은 차트 라이브러리를 도입하여 시계열 인구 변화를 라인/바 차트로 구현합니다.
        *   **지도**: `Leaflet`, `Kakao Map API` 등의 GIS 라이브러리를 도입하고, 제주 읍면동 경계 GeoJSON 데이터를 확보하여 인구 밀도 등에 따라 색상을 달리하여 표현하는 **단계 구분도(Choropleth Map)**를 구현합니다.

### 다. 구체적인 실행 계획 (Action Plan)

| 단계 | 태스크 (Task) | 세부 실행 내용 | 기술 스택/라이브러리 | 우선순위 |
| :--- | :--- | :--- | :--- | :--- |
| **1** | **백엔드 프록시 구축** | KOSIS API 호출을 중계할 엔드포인트(예: `/api/population`) 개발 | `Node.js` 또는 `Vercel Serverless Function` | **P0** |
| **2** | **데이터 패칭 구현** | `PopulationInfoTab.tsx`에서 백엔드 프록시 호출, 로딩/에러 상태 처리 | `axios`, `React Query/SWR` (선택) | **P0** |
| **3** | **차트 라이브러리 도입** | 인구 데이터를 시계열 라인 차트로 시각화하는 컴포넌트 개발 | `Recharts` 또는 `Nivo` | **P0** |
| **4** | **GIS 지도 연동** | 제주 읍면동 GeoJSON 확보 및 지도 위에 인구 데이터 시각화 | `react-leaflet`, `Kakao Map API` | **P1** |
| **5** | **데이터 가공/매핑** | KOSIS API 응답 데이터를 차트/지도에 맞게 가공하는 유틸리티 함수 작성 | `TypeScript` | **P1** |
| **6** | **데이터 캐싱/스케줄링** | KOSIS 데이터 갱신 주기에 맞춰 백엔드에서 데이터를 주기적으로 캐싱하는 전략 수립 | `cron`, `Redis` (확장 시) | **P2** |

---

## 1. 제품 개요 (Product Overview)

*   **개발 배경:** 제주도는 대한민국의 대표적인 관광지이자 이주 선호 지역으로, 인구 유입이 활발하지만 특정 지역은 인구 소멸 위기에 직면하는 등 인구 구조의 동적인 변화를 겪고 있습니다. 이러한 변화를 체계적으로 분석하여 정책 수립, 비즈니스 전략, 이주 계획에 활용할 수 있는 데이터 기반의 인사이트가 필요합니다. 'Jeju Pop-Radar'는 제주 지역의 인구 동향을 시각적으로 분석하고, 시계열 데이터를 제공하여 이러한 필요성을 충족시키기 위해 개발되었습니다.
*   **제품명:** 제주 지역별 인구동향 분석 시스템 (가칭: Jeju Pop-Radar)
*   **타겟 유저 및 핵심 가치:**
    *   **제주특별자치도 정책 입안자:** 읍면동 단위의 상세한 인구 데이터를 기반으로 균형 발전, 인구 유입 정책, 복지 서비스 분배 등 효과적인 정책을 수립할 수 있습니다.
    *   **지역 소상공인 및 비즈니스 마케터:** 특정 지역의 인구 밀도, 연령대별 인구 분포, 유입/유출 인구 특성을 파악하여 신규 출점, 타겟 마케팅, 서비스 개발 등 비즈니스 기회를 포착할 수 있습니다.
    *   **제주 이주 희망자:** 실제 거주민 데이터를 바탕으로 정착 지역을 탐색하고, 생활 인프라, 교육 환경 등을 고려한 합리적인 이주 계획을 세울 수 있습니다.

## 2. KOSIS 연동 중심의 기능 요구사항 (Functional Requirements)

*   **분석 공간 단위:** 행정시(제주시/서귀포시) 및 읍면동 단위
*   **데이터 핵심 주기:** 월간(Monthly) 및 연간(Yearly) 시계열 데이터
*   **기능별 우선순위:**
    *   **P0 (필수 구현):**
        *   KOSIS API 연동 및 데이터 수집 파이프라인 구축
        *   읍면동 단위 인구 데이터 (주민등록인구통계) 시각화
        *   월간/연간 시계열 데이터 조회 및 차트 시각화
        *   제주도 GIS 데이터와 인구 데이터 매핑 및 지도 시각화
    *   **P1 (권장):**
        *   인구이동(A3), 인구밀도(AT) 등 추가 지표 조회 기능
        *   KOSIS 통계목록 트리 구조 탐색 기능
        *   지도 인터랙션 (마우스 오버 시 읍면동별 상세 정보 팝업)
    *   **P2 (추후 확장):**
        *   사용자 정의 데이터 업로드 및 비교 분석
        *   AI 기반 인구 예측 모델링 기능

*   **기능 기술서 (User Stories):**
    *   **As a** 정책 입안자, **I want to** 읍면동별 인구 변화를 월별/연도별 시계열 차트로 확인하고 싶습니다. **So that** 인구 소멸 위기 지역을 조기에 발견하고 지원 정책을 마련할 수 있습니다.
    *   **As a** 비즈니스 마케터, **I want to** 지도에서 특정 지역을 클릭했을 때 해당 지역의 인구 밀도와 주요 연령층 정보를 보고 싶습니다. **So that** 신규 매장 입지 선정에 활용할 수 있습니다.
    *   **As a** 제주 이주 희망자, **I want to** 제주시와 서귀포시의 최근 5년간 인구 증감률을 비교하고 싶습니다. **So that** 나의 라이프스타일에 맞는 거주 지역을 선택할 수 있습니다.

*   **백엔드 데이터 가이드 (KOSIS API 연동):**
    *   **1. 통계목록 조회 파이프라인 (가이드 2.1):**
        *   **URL:** `https://kosis.kr/openapi/statisticsList.do`
        *   **Params:** `method=getList`, `vwCd=MT_ZTITLE`, `parentListId={상위ID}`
        *   **Description:** 인구이동(A3), 주민등록인구통계, 인구밀도(AT) 등 관련 통계표의 상위/하위 목록을 동적으로 탐색하는 기능을 위해 사용.
    *   **2. 통계자료 수집 및 데이터 조인 (가이드 2.2 & 2.3):**
        *   **URL:** `https://kosis.kr/openapi/statisticsData.do`
        *   **Params:**
            *   `apiKey={발급받은_API_키}`
            *   `format=json` (필수)
            *   `jsonVD=Y` (필수, 값에 대한 설명 함께 요청)
            *   `userStatsId={사용자정의_통계ID}` (백엔드에서 관리)
            *   `prdSe={Y or M}` (수록주기: 연간/월간)
            *   `startPrdDe={시작시점}`, `endPrdDe={종료시점}`
            *   `newEstPrdCnt=1` (최신 데이터 1개만 조회 시)
            *   `orgId`, `tblId` 등 특정 통계표를 식별하는 ID
        *   **Description:** 실제 통계 데이터를 JSON 형식으로 수집. 대용량 데이터는 `statisticsBigData.do` 사용을 고려.
    *   **3. 데이터 매핑용 메타자료 및 주석 연동 (가이드 2.5):**
        *   **URL:** `https://kosis.kr/openapi/statisticsData.do`
        *   **Params:**
            *   `apiKey={발급받은_API_키}`
            *   `format=json`
            *   **분류/항목:** `type=ITM`, `orgId`, `tblId`, `objId`, `itmId`
            *   **단위:** `type=UNIT`, `orgId`, `tblId`
            *   **자료갱신일:** `type=NCD`, `orgId`, `tblId`
        *   **Description:**
            *   KOSIS 분류코드(`ITM_ID`)를 행정안전부 행정동 코드와 매핑 테이블을 구축하여 GIS 데이터와 조인.
            *   시각화 차트/지도에 `UNIT_NM`을 동적으로 표시하고, 대시보드에 `자료갱신일`을 명시하여 데이터 신뢰도 확보.

## 3. 화면 설계 및 시각화 방향 (UI/UX Concepts)

*   **메인 대시보드 구조:**
    *   **지도 중심 인터페이스:** 화면의 중심에 제주도 읍면동 단위의 폴리곤(Polygon) 지도를 배치.
    *   **인터랙티브 시각화:**
        *   **On-hover:** 특정 읍면동 폴리곤에 마우스 커서를 올리면, 해당 지역명과 핵심 인구 지표(총인구, 인구밀도 등)가 툴팁(Tooltip) 또는 팝업 형태로 표시됨.
        *   **On-click:** 특정 읍면동을 클릭하면, 화면의 다른 영역(또는 하단)에 해당 지역의 상세 시계열 데이터 차트가 렌더링됨.
    *   **시계열 데이터 차트:**
        *   라인 차트(Line Chart)를 기본 형태로 하여 시간(월/년)에 따른 인구 변화 추이를 시각화.
        *   ggplot, Matplotlib 스타일의 깔끔하고 직관적인 차트 디자인을 지향.
        *   여러 지역을 선택하여 차트에서 비교 분석할 수 있는 기능 제공.
*   **동적 UI 요소:**
    *   **차트 제목 및 축 레이블:** KOSIS 메타자료 API(`type=ITM`)를 통해 받은 `ITM_NM`을 차트의 제목이나 범례(Legend)에 동적으로 활용.
    *   **단위 표시:** Y축이나 툴팁에 표시되는 단위(예: 명, 세대, %)를 메타자료 API(`type=UNIT`)에서 받은 `UNIT_NM`으로 동적으로 출력하여 데이터의 명확성을 높임.
    *   **데이터 출처 및 갱신일:** 대시보드 하단이나 정보 아이콘(i) 클릭 시, "자료 출처: KOSIS 국가통계포털"과 함께 메타자료 API(`type=NCD`)에서 받은 `자료갱신일`을 명시.

## 4. 비기능적 요구사항 (Non-Functional Requirements)

*   **성능 (Performance):**
    *   **초기 로딩 속도:** 메인 대시보드의 초기 렌더링(TTV, TTI)은 3초 이내를 목표로 함. 지도 데이터(GeoJSON)와 초기 인구 데이터는 압축하여 제공하고, 비동기적으로 로드.
    *   **API 응답:** KOSIS API 호출 응답 시간은 네트워크 상황에 따라 변동될 수 있으므로, 로딩 상태(스피너, 스켈레톤 UI)를 명확히 표시하여 사용자 경험 저하를 방지.
*   **안정성 및 가용성 (Reliability & Availability):**
    *   **KOSIS API 장애 대응:** KOSIS API 서버가 불안정하거나 점검 중일 경우를 대비하여, 가장 최근에 성공적으로 수집한 데이터를 캐시(Cache)하거나 DB에 저장하여 서빙하는 폴백(Fallback) 정책을 수립. (e.g., Redis, in-memory cache)
    *   **예외 처리:** API 호출 실패, 데이터 파싱 오류 등 발생 가능한 모든 예외 상황에 대한 로깅 및 사용자 알림(e.g., "데이터를 불러오는 데 실패했습니다. 잠시 후 다시 시도해주세요.") 처리.
*   **데이터 관리 (Data Management):**
    *   **배치 스케줄러:** 매월 1회, KOSIS의 자료갱신일 API(`type=NCD`)를 주기적으로 체크하는 스케줄러(e.g., cron job)를 백엔드에 설계. 갱신이 확인되면, 새로운 데이터를 자동으로 마이그레이션하여 DB를 최신 상태로 유지.
*   **보안 (Security) 및 기술 제약:**
    *   **크로스 도메인 (CORS):** 브라우저에서 직접 KOSIS API를 호출할 때 발생하는 CORS(Cross-Origin Resource Sharing) 문제를 해결해야 함.
        *   **해결 방안 1 (권장):** 백엔드에 프록시(Proxy) API 엔드포인트를 만들어, 프론트엔드는 이 백엔드 엔드포인트를 호출하고, 백엔드 서버가 KOSIS API와 통신하도록 구현.
        *   **해결 방안 2:** 서버리스 함수(e.g., Netlify Functions, Vercel Serverless Functions)를 프록시로 활용.

## 5. 프로젝트 마일스톤 및 핵심 지표 (KPI)

*   **MVP 개발 마일스톤:**
    *   **1단계 (1주차): 환경 설정 및 API 연동:**
        *   KOSIS API 키 발급 및 개발 환경 설정.
        *   백엔드 프록시 API 구현 및 `statisticsData.do` 호출 테스트.
    *   **2단계 (2-3주차): 데이터 마이그레이션 및 매핑:**
        *   주민등록인구통계 데이터 수집 및 DB 저장 스키마 설계.
        *   배치 스케줄러 초기 버전 구현.
        *   행정동 코드와 KOSIS 분류 코드 매핑 테이블 구축.
    *   **3단계 (4주차): GIS 지도 매핑 및 시각화:**
        *   제주도 읍면동 GeoJSON 데이터 확보 및 지도 라이브러리(e.g., Leaflet, Kakao Map API) 연동.
        *   DB의 인구 데이터를 지도 폴리곤과 조인하여 색상으로 표현 (Choropleth map).
    *   **4단계 (5-6주차): 대시보드 UI/UX 구현 및 오픈:**
        *   인터랙티브 차트 및 툴팁 기능 구현.
        *   시계열 데이터 조회 필터(기간 설정) UI 개발.
        *   최종 테스트 및 MVP 버전 배포.

*   **핵심 지표 (KPI):**
    *   **시스템 성능:**
        *   월간 데이터 자동 갱신 성공률: 99.9% 이상
        *   API 프록시 평균 응답 시간: 500ms 이하
        *   초기 페이지 로드 시간 (LCP): 2.5초 이하
    *   **비즈니스/사용자:**
        *   MAU (월간 활성 사용자 수): 500명 이상 (초기 3개월 목표)
        *   주요 기능(지역별 조회, 시계열 분석) 사용 빈도
        *   사용자 피드백을 통한 만족도 점수 (5점 만점에 4점 이상)
