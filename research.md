
# PDF 데이터 추출 로직 개선 영향도 분석

## 1. 목표

- `OmsSystem.tsx` 컴포넌트의 '새 보고서 업로드' 기능이 월간 공과금 PDF 파일로부터 데이터를 정확하게 추출하지 못하는 문제를 해결합니다.
- 최종 목표는 프롬프트에서 명시한 JSON 구조와 완전히 일치하는 데이터를 안정적으로 추출하고, 이를 화면에 올바르게 표시하는 것입니다.
- 이 과정에서 **기존의 다른 기능(자산 가치 지수 등)에 어떠한 영향도 주지 않고**, 오직 데이터 추출 로직만을 수정하는 데 집중합니다.

## 2. 영향 받는 파일

- **수정 대상:** `src/components/oms/OmsSystem.tsx`
- **수정 내용:** 파일 내의 PDF 파싱 및 데이터 추출 처리 로직 (`handleUpload` 또는 `parsePdf` 함수)에 한정됩니다.
- **영향 없음:** `AssetManagement.tsx`를 포함한 다른 모든 컴포넌트, 라우팅, 상태 관리 로직은 전혀 영향을 받지 않습니다.

## 3. 구현 계획

1.  **정확한 열(Column) 식별:** PDF 파싱 시, 텍스트의 x, y 좌표를 활용하여 '금월 사용량'에 해당하는 중앙 열의 위치를 명확하게 특정합니다. '전월 사용량'에 해당하는 맨 오른쪽 열의 데이터는 의도적으로 무시하도록 로직을 수정합니다.
2.  **안정적인 레이블-값 매칭:** 각 항목(예: '기본요금', '전력량요금')의 레이블 텍스트를 찾고, 해당 레이블과 동일한 y 좌표(또는 매우 근접한 y 좌표)를 가지면서 '금월 사용량' 열 범위 내에 있는 숫자 값을 찾아 매칭합니다. 이를 통해 현재 부정확하게 추출되는 값들을 바로잡습니다.
3.  **데이터 형식 변환:** 추출된 텍스트 값에서 쉼표(,)를 제거하고, 숫자(정수 또는 실수)로 변환합니다. 값이 하이픈(-)이거나 없는 경우에는 0으로 처리하여 JSON 구조의 데이터 타입을 일관성 있게 유지합니다.
4.  **섹션별 분리 파싱:** 전기, 수도, 가스 요금 섹션의 구조가 미세하게 다를 수 있으므로, 각 섹션을 구분하여 독립적으로 파싱하는 로직을 적용해 안정성을 높입니다.
5.  **최종 JSON 구조화:** 모든 데이터 추출이 완료되면, 그 결과를 프롬프트에서 요구한 것과 **정확히 동일한 JSON 객체 구조**로 조립합니다.
6.  **UI 업데이트:** 기존의 부정확한 데이터 표시 부분을, 새로 생성된 JSON 객체를 `JSON.stringify`를 사용해 포맷팅하여 `<pre><code>` 태그 내에 표시하는 방식으로 교체합니다. 이는 최종 결과물 스크린샷과 일치하는 방식입니다.

## 4. 예상되는 영향 및 리스크

- **사용자 경험 (긍정적):** '새 보고서 업로드' 기능이 정상적으로 작동하여, 사용자는 PDF를 업로드하기만 하면 정확하게 추출된 공과금 데이터를 JSON 형식으로 즉시 확인할 수 있게 됩니다. 데이터 신뢰도가 크게 향상됩니다.
- **코드 변경 (제한적):** 코드 수정은 `OmsSystem.tsx` 파일 내의 특정 함수로 범위가 명확하게 한정되므로, 다른 기능에 사이드 이펙트를 유발할 가능성이 매우 낮습니다.
- **기능 유실 리스크 (없음):** 이번 수정 작업은 기존 UI의 레이아웃이나 다른 탭의 기능을 변경하지 않습니다. 오직 데이터 처리 로직과 그 결과를 표시하는 부분만 개선되므로 **기능 유실의 리스크는 없습니다.**
- **잠재적 리스크:** 현재의 파싱 로직은 제공된 PDF의 특정 레이아웃에 의존합니다. 만약 향후 다른 형식의 공과금 청구서 PDF가 사용될 경우, 레이아웃 차이로 인해 데이터 추출이 실패할 수 있습니다. 이는 새로운 형식의 PDF에 맞춰 파싱 로직을 추가로 조정해야 하는 유지보수 포인트가 될 수 있습니다.
