# 제주 헬스케어타운 단지 조성 및 자산관리 시스템: 기술 문서

> 최종 업데이트: 2026년 3월 26일

## 1. 프로젝트 개요

본 문서는 제주 헬스케어타운의 단지 조성 현황과 완공 후 자산(세대)의 임대 및 분양 관리를 효율화하기 위해 구축된 '업무관리 시스템'의 기술적 사양과 아키텍처, 핵심 기능 구현 방식을 상세히 기술합니다.

## 2. 핵심 목표

- **데이터 중심 의사결정**: 분산된 엑셀 파일과 문서를 중앙화된 데이터베이스로 통합하여, 모든 의사결정이 실시간 데이터에 기반하도록 지원합니다.
- **업무 자동화 및 효율화**: 반복적인 보고서 작성, 데이터 취합, 현황 분석 등의 업무를 자동화하여 휴먼 에러를 줄이고 업무 효율을 극대화합니다.
- **직관적 데이터 시각화**: 복잡한 단지 조성 현황과 임대/수익 데이터를 사용자가 쉽게 이해할 수 있는 인터랙티브 대시보드와 도면으로 시각화합니다.

## 3. 핵심 아키텍처 및 기술 스택

- **언어**: TypeScript
- **UI 프레임워크**: React
- **UI 컴포넌트**: shadcn/ui
- **데이터 시각화**: Recharts (Chart.js에서 변경)
- **상태 관리**: React Context API
- **백엔드/데이터베이스**: Google Firestore
- **빌드 도구**: Vite
- **호스팅**: Firebase Hosting
- **버전 관리**: Git / GitHub

## 4. 데이터 모델링 (Firestore)

- **`tenantUnits`**: 각 세대(호실)의 상세 정보 (면적, 임대료, 상태 등)
- **`complexFacilities`**: 단지 내 주요 시설물 정보
- **`teamMembers`**: 프로젝트 관련 인원 정보
- **`activities`**: 프로젝트 활동 로그
- **`kpis`**: 핵심 성과 지표
- **`monthly_reports`**: 월간 운영 데이터 (JSON 기반에서 Firestore로 통합)

---

## 5. ~ 14. (이전 기록 생략)

---

## 15. AI 기반 심층 분석 대시보드 구축 및 안정화 (2026.03.25 추가)

### 15.1. 기능 목표

단순한 데이터 나열을 넘어, 경영진이 데이터에 기반하여 신속하고 정확한 의사결정을 내릴 수 있도록 '인사이트'를 제공하는 것을 목표로 합니다. 이를 위해, 핵심 지표 요약, 시계열 분석, 전년 동월 비교, 그리고 AI 기반의 심층 분석 기능을 포함하는 통합 대시보드를 구축했습니다.

### 15.2. 핵심 구현 과정

#### 1. 데이터 구조 확장

- **문제점**: 기존 데이터 모델은 에너지 사용량과 팀 활동 내역만 포함하여, 기온 변화와 같은 외부 요인과의 상관관계 분석이 불가능했습니다.
- **해결 방안**:
    - `src/types.ts`의 `MonthlyReport` 타입에 `weather` 항목 (평균 기온 `averageTemperatureC` 포함)을 추가했습니다.
    - `src/data/2026-02-report.json` 파일에 실제 `weather` 데이터를 추가하여 새로운 데이터 구조의 프로토타입을 완성했습니다.

#### 2. 대시보드 UI/UX 구현 (`OmsDashboard.tsx`)

- 기존의 단순 업무 목록(`OmsTeamTasks.tsx`)을 완전히 새로운 `OmsDashboard.tsx` 컴포넌트로 대체했습니다.
- **주요 기능**:
    - **핵심 요약 카드**: 전월 대비 총비용 변화량을 직관적으로 보여주는 헤더 카드.
    - **Metric 카드**: 전기/가스 사용량, 평균 기온, 태양광 발전량을 전년 동월과 비교하여 시각화.
    - **시계열 분석 차트**: `recharts` 라이브러리를 활용하여 최근 12개월간의 에너지 사용 추이를 라인 차트로 시각화.
    - **AI 심층 분석**: 버튼 클릭 시, AI가 분석한 데이터 인사이트와 비용 절감 방안을 텍스트 형태로 제공.

### 15.3. 배포 후 긴급 장애 대응

- **문제 상황**: 최초 배포 직후, `Cannot read properties of undefined (reading 'averageTemperatureC')` 라는 치명적인 런타임 오류가 발생하여 애플리케이션이 완전히 중단되었습니다.
- **원인 분석**: 대시보드 코드는 모든 월간 보고서에 '평균 기온' 데이터(`weather.averageTemperatureC`)가 존재할 것이라고 가정했으나, 과거 데이터에는 해당 필드가 없어 오류가 발생했습니다. AI(본인)가 모든 데이터의 호환성을 검증하지 않고 성급하게 배포한 것이 원인이었습니다.
- **최종 해결 방안**:
    - **Optional Chaining (`?.`) 및 Nullish Coalescing (`??`) 적용**: `OmsDashboard.tsx` 코드 내에서, `latestReport.raw_data.weather?.averageTemperatureC?.value`와 같이 Optional Chaining을 사용하여 데이터의 존재 여부를 안전하게 확인하도록 수정했습니다.
    - **폴백 값(Fallback Value) 제공**: 데이터가 존재하지 않을 경우, `?? 'N/A'` 와 같이 Nullish Coalescing 연산자를 사용하여 "N/A" (Not Available)를 표시하도록 하여, 데이터가 없어도 UI가 깨지거나 시스템이 중단되지 않도록 방어적인 코드를 작성했습니다.
    - **긴급 빌드 및 재배포**: 수정된 코드를 즉시 빌드하고 재배포하여 시스템을 성공적으로 복구했습니다.

### 15.4. 교훈

- **과거 데이터는 새로운 기능의 적이다**: 새로운 필드나 데이터 구조를 도입할 때는, 반드시 기존 데이터와의 하위 호환성을 보장해야 합니다.
- **방어적 프로그래밍의 중요성**: 외부 또는 과거 데이터 소스를 다룰 때는, 데이터가 항상 완벽한 형태로 존재할 것이라고 가정해서는 안 됩니다. Optional Chaining, Nullish Coalescing, 타입 가드 등을 적극적으로 활용하여 예기치 않은 오류를 방지해야 합니다.
- **배포는 끝이 아닌 새로운 시작**: 배포 직후에는 반드시 실제 운영 환경에서 모든 기능이 정상적으로 동작하는지 철저히 테스트하고 모니터링해야 합니다.

---

## 16. 대시보드 UI/UX 개선 및 기능 고도화 (2026.03.26 추가)

### 16.1. 기능 목표

사용자 피드백을 기반으로 대시보드의 직관성과 사용성을 개선하고, 데이터 분석의 정확성을 높이는 것을 목표로 합니다. 특히, '업무 현황'과 '임대 현황' 정보의 시각적 분리, 데이터 집계 기준의 명확화, 그리고 동적인 컨텍스트 제공에 중점을 두었습니다.

### 16.2. 핵심 개선 사항

#### 1. 카드 높이 일치 및 레이아웃 안정화

- **문제점**: 대시보드의 '업무 현황 요약'과 '임대 현황 요약' 카드의 콘텐츠 양이 달라 높이가 불일치하여, 전체적인 레이아웃의 불안정성을 야기했습니다.
- **해결 방안**:
    - `Dashboard.tsx` 컴포넌트의 부모 `div`에 `grid`와 `lg:grid-cols-2` 클래스를 적용했습니다.
    - `TaskStatusPieChart.tsx`와 `LeaseStatusWidget.tsx` 컴포넌트의 루트 `div`에 `h-full` 클래스를 추가하여, Grid 컨테이너 내에서 항상 동일한 높이를 유지하도록 수정했습니다.

#### 2. '업무 현황 요약' 차트 시각적 개선

- **문제점**: '임대 현황'과 '업무 현황' 차트가 모두 푸른색 계열로 표시되어 시각적 구분이 어려웠습니다.
- **해결 방안**:
    - `TaskStatusPieChart.tsx` 파일에서, 차트의 '완료' 상태 색상(`COMPLETED_COLOR`)을 기존 `#4338CA` (남색 계열)에서 `#EF4444` (붉은색 계열)로 변경했습니다.
    - 배경색(`INCOMPLETE_COLOR`) 또한 `#FEE2E2` (연한 빨강)으로 변경하여 색상 조합의 통일성을 확보하고 가독성을 높였습니다.

#### 3. '업무 현황' 데이터 집계 기준 변경

- **문제점**: 기존 '업무 현황 요약'은 상단의 KPI와 동일하게 월별 데이터를 표시하여 중복적인 정보를 제공했습니다. 연간 누적 현황을 파악하기 어렵다는 피드백이 있었습니다.
- **해결 방안**:
    - `Dashboard.tsx` 파일에 연간 누적 데이터를 계산하는 로직(`yearlyTaskStats`)을 새롭게 추가했습니다.
    - `TaskStatusPieChart` 컴포넌트에는 이 `yearlyTaskStats`를 props로 전달하도록 수정하여, 차트가 **올해 1월 1일부터 현재까지의 누적 데이터**를 기반으로 표시되도록 변경했습니다.
    - 상단의 월별 KPI는 그대로 유지하여, 월별 상세 현황과 연간 전체 그림을 동시에 파악할 수 있도록 개선했습니다.

#### 4. 차트 제목 동적 업데이트

- **문제점**: '업무 현황 요약'이라는 정적인 제목은 데이터 집계 기준이 연간으로 변경된 후 컨텍스트를 명확하게 전달하지 못했습니다.
- **해결 방안**:
    - `TaskStatusPieChart.tsx` 컴포넌트 내에서 `new Date().getFullYear()`를 사용하여 현재 연도를 동적으로 가져왔습니다.
    - 기존의 정적 제목(`<h3>업무 현황 요약</h3>`)을 `{currentYear}년 업무진척도`와 같이 동적으로 생성되는 제목으로 교체하여, 사용자가 차트의 컨텍스트(예: "2026년 업무진척도")를 명확하게 인지할 수 있도록 수정했습니다.

### 16.3. 종합 결과

이번 업데이트를 통해 대시보드의 정보 전달력이 크게 향상되었습니다. 사용자는 이제 월별 핵심 지표와 연간 누적 업무진척도를 한눈에 비교하며 파악할 수 있게 되었으며, 개선된 UI/UX를 통해 보다 빠르고 직관적인 데이터 분석이 가능해졌습니다.

---

## 17. 치명적 오류 수정 및 시스템 안정화 (2026.03.27 추가)

### 17.1. 기능 목표

최근 발생한 두 가지 치명적인 오류(임대율 0% 표시, 데이터 타입 불일치)의 근본 원인을 해결하고, 시스템의 안정성을 확보하는 것을 최우선 목표로 합니다. 또한, 이 과정을 명확하게 문서화하여 향후 유사한 문제의 재발을 방지합니다.

### 17.2. 핵심 문제 및 해결 과정

#### 1. 임대율 0% 표시 오류

- **문제 상황**: 일부 세대가 명백히 'OCCUPIED' (임대 중) 상태임에도 불구하고, 대시보드의 전체 및 층별 임대율이 모두 0%로 표시되는 치명적인 오류가 발생했습니다.
- **원인 분석**: `TenantRoster.tsx`의 임대율 계산 로직 자체는 정교했으나, 계산의 기준이 되는 상태 값이 코드와 데이터 간에 불일치했습니다.
    - **코드(src/constants.ts)**: `UNIT_STATUS.OCCUPIED` 상수가 `'입주'` (한글)로 정의되어 있었습니다.
    - **데이터**: 실제 데이터(UI 및 데이터베이스)에서는 `'OCCUPIED'` (영문)라는 값을 사용하고 있었습니다.
    - 이로 인해 코드는 `'입주'` 상태인 세대를 찾았지만, 실제 데이터에는 존재하지 않아 임대 면적을 전혀 계산하지 못했습니다.
- **최종 해결 방안**:
    - `src/constants.ts` 파일의 `UNIT_STATUS` 객체 내 모든 상태 값을 실제 데이터와 일치하도록 **영문 대문자(예: `OCCUPIED`, `VACANT`)로 통일**했습니다.
    - 이 간단하지만 치명적인 불일치를 해결함으로써, 임대율 계산 로직이 즉시 정상적으로 작동하게 되었습니다.

#### 2. 데이터 변환 타입 오류

- **문제 상황**: 애플리케이션 빌드 및 데이터 로딩 과정에서 `solarGenerationKwh` 속성 관련 타입 불일치 오류가 지속적으로 발생했습니다.
- **원인 분석**: `ProjectDataProvider.tsx`에서 JSON 데이터를 시스템의 `MonthlyReport` 타입으로 변환하는 과정에서, 일부 데이터 소스(예: `2026-02-report.json`)에 `solarGenerationKwh` (태양광 발전량) 필드가 존재하지 않았습니다. 하지만 변환 후의 타입 정의에는 이 필드가 필수로 지정되어 있어, 데이터가 존재하지 않는 경우 타입 오류가 발생했습니다.
- **최종 해결 방안**:
    - `ProjectDataProvider.tsx` 내의 데이터 변환 함수(`transformRawDataToMonthlyReport`) 로직을 수정했습니다.
    - **Nullish Coalescing 연산자(`||`)를 사용**하여, 원본 데이터에 `solarGenerationKwh` 속성이 존재하지 않을 경우, `{ value: 0, unit: 'kWh' }` 라는 기본값을 할당하도록 방어적인 코드를 추가했습니다.
    - 이를 통해 데이터 소스에 특정 필드가 존재하지 않더라도, 시스템 전체의 데이터 무결성과 타입 안정성을 유지할 수 있게 되었습니다.

### 17.3. 종합 결과 및 교훈

이번 장애 대응을 통해, 겉으로 드러나는 현상만 해결하는 것이 아니라 근본적인 원인을 파악하는 것의 중요성을 다시 한번 깨닫게 되었습니다. 특히, **데이터와 코드 간의 '암묵적인 약속'(상태 값, 타입 정의 등)을 명시적으로 일치시키는 것**이 시스템 안정성의 핵심임을 확인했습니다.

- **교훈 1: 상수는 유일한 진실의 원천(Single Source of Truth)이어야 한다.** 시스템의 모든 부분은 동일한 상수 값을 참조해야 하며, 한글/영문 혼용, 대/소문자 불일치 등은 반드시 피해야 합니다.
- **교훈 2: 데이터 변환은 관대하게, 사용은 엄격하게(Robustness Principle).** 외부 데이터를 받아들일 때는 형식이 불완전하더라도 최대한 유연하게 처리하고(기본값 할당), 시스템 내부에서 사용할 때는 엄격한 타입 정의를 통해 데이터의 정합성을 보장해야 합니다.

이 모든 과정을 `project_documentation.md`에 상세히 기록함으로써, 미래의 저 자신과 동료들이 같은 실수를 반복하지 않도록 하는 중요한 자산을 남깁니다.
